spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssafyhome?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8
    username: ssafy
    password: ssafy

mybatis:
  type-aliases-package: com.ssafy.*.model
  mapper-locations: mapper/**/*.xml

logging:
  level:
    root: info
    com:
      ssafy: debug
      zaxxer:
        hikari:
          pool: debug
